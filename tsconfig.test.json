{
  "$comment": "Nur für den Prüfstand unter test/. Ausserhalb von Next.js gibt es kein 'server-only' — der Verweis unten ersetzt es durch ein leeres Modul.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "module": "esnext",
    "moduleResolution": "bundler",
    "paths": {
      "@/*": ["./src/*"],
      "server-only": ["./test/server-only.ts"]
    }
  }
}
